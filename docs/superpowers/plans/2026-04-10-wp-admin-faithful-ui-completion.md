# WP Admin UI Faithful Reproduction + E2E + CLI Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the generated Next.js admin pages visually match WordPress admin UI, add comprehensive E2E tests for all 22 PHP scripts, and polish the CLI workflow for one-command migration verification.

**Architecture:** Replace all inline styles in `admin-scaffold-generator.ts` with WP-admin-faithful CSS classes via a generated `globals.css`. Extract reusable admin components (`AdminLayout`, `AdminNotice`, `DataTable`, `FormField`, `LoadingButton`) into generated `components/admin/` files. Extend `verify-generator.ts` with authenticated CRUD tests. Improve `run.ts` with migration coverage reporting.

**Tech Stack:** TypeScript, React 19, Next.js 15 App Router, Prisma, Playwright, vitest

---

## File Structure

### New files (generated by admin-scaffold-generator)
- `app/globals.css` — WP admin color palette + base typography + component classes
- `components/admin/AdminNotice.tsx` — Success/warning/error notification banner
- `components/admin/DataTable.tsx` — Sortable, paginated table with bulk actions
- `components/admin/FormField.tsx` — Label + input + validation message wrapper
- `components/admin/LoadingButton.tsx` — Button with spinner state

### Modified files
- `packages/analyzer/src/admin-scaffold-generator.ts` — Replace inline styles with CSS classes, generate component files, generate globals.css
- `packages/analyzer/tests/admin-scaffold-generator.test.ts` — Test WP-style output
- `packages/analyzer/src/verify-generator.ts` — Enhanced CRUD tests with auth + response body validation
- `packages/analyzer/tests/verify-generator.test.ts` — Test new verify specs
- `apps/cli/src/commands/run.ts` — Migration coverage report, error recovery guidance

---

## Task 1: WP Admin CSS Generation

**Files:**
- Modify: `packages/analyzer/src/admin-scaffold-generator.ts:1-10`
- Test: `packages/analyzer/tests/admin-scaffold-generator.test.ts`

### Step 1: Write failing test for globals.css generation

- [ ] **Step 1.1: Add test**

```typescript
// Add to packages/analyzer/tests/admin-scaffold-generator.test.ts

describe("globals.css generation", () => {
  it("generates WP-admin-style globals.css with color palette", () => {
    const analyses = [makeAnalysis({ fileName: "page-event-list.php" })];
    const tables = [makeTable("event")];

    const pages = generateAdminScaffold(analyses, tables);
    const globalsCss = pages.find((p) => p.path === "app/globals.css");

    expect(globalsCss).toBeDefined();
    expect(globalsCss!.content).toContain("#23282d"); // WP sidebar dark
    expect(globalsCss!.content).toContain("#0073aa"); // WP primary blue
    expect(globalsCss!.content).toContain("#46b450"); // WP success green
    expect(globalsCss!.content).toContain("#dc3232"); // WP error red
    expect(globalsCss!.content).toContain(".wp-admin-sidebar");
    expect(globalsCss!.content).toContain(".wp-list-table");
    expect(globalsCss!.content).toContain(".wp-notice");
  });
});
```

- [ ] **Step 1.2: Run test to verify it fails**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts -t "generates WP-admin-style globals.css"`
Expected: FAIL — no globals.css in output

- [ ] **Step 1.3: Implement generateGlobalsCss()**

Add this function before the `generateAdminScaffold` function in `packages/analyzer/src/admin-scaffold-generator.ts` (before line 1518):

```typescript
function generateGlobalsCss(): string {
  return `/* WP Admin Faithful CSS — Generated by wp-transfer */

/* ── Reset & Base ── */
*, *::before, *::after {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", sans-serif;
  font-size: 14px;
  line-height: 1.4em;
  color: #3c434a;
  background: #f0f0f1;
}

/* ── WP Color Palette ── */
:root {
  --wp-admin-dark: #23282d;
  --wp-admin-darker: #1d2327;
  --wp-admin-light: #32373c;
  --wp-primary: #0073aa;
  --wp-primary-hover: #006799;
  --wp-primary-focus: #007cba;
  --wp-accent: #00a0d2;
  --wp-success: #46b450;
  --wp-warning: #ffb900;
  --wp-error: #dc3232;
  --wp-info: #00a0d2;
  --wp-border: #c3c4c7;
  --wp-border-light: #dcdcde;
  --wp-bg: #f0f0f1;
  --wp-text: #3c434a;
  --wp-text-light: #646970;
}

/* ── Sidebar ── */
.wp-admin-sidebar {
  width: 240px;
  background-color: var(--wp-admin-dark);
  color: #c3c4c7;
  min-height: 100vh;
  flex-shrink: 0;
}

.wp-admin-sidebar-header {
  padding: 16px 12px;
  font-size: 16px;
  font-weight: 600;
  color: white;
  border-bottom: 1px solid var(--wp-admin-light);
}

.wp-admin-menu {
  list-style: none;
  margin: 0;
  padding: 8px 0;
}

.wp-admin-menu-item {
  display: block;
  padding: 8px 12px;
  color: #c3c4c7;
  text-decoration: none;
  font-size: 14px;
  border-left: 3px solid transparent;
  transition: background-color 0.1s, border-color 0.1s;
}

.wp-admin-menu-item:hover {
  background-color: var(--wp-admin-light);
  color: #fff;
}

.wp-admin-menu-item.active {
  background-color: var(--wp-primary);
  color: white;
  border-left-color: white;
}

/* ── Main Content ── */
.wp-admin-content {
  flex: 1;
  background-color: var(--wp-bg);
  overflow: auto;
}

.wp-admin-wrap {
  padding: 20px;
  max-width: 1200px;
}

/* ── Header ── */
.wp-admin-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}

.wp-admin-title {
  font-size: 23px;
  font-weight: 400;
  color: var(--wp-text);
  line-height: 1.3;
  margin: 0;
  padding: 9px 0 4px;
}

.wp-admin-title-action {
  display: inline-block;
  padding: 4px 8px;
  font-size: 13px;
  color: var(--wp-primary);
  border: 1px solid var(--wp-primary);
  border-radius: 3px;
  text-decoration: none;
  margin-left: 10px;
}

.wp-admin-title-action:hover {
  background-color: var(--wp-primary);
  color: white;
}

/* ── Notices ── */
.wp-notice {
  border-left: 4px solid var(--wp-primary);
  padding: 12px 16px;
  margin: 5px 0 15px;
  background: #fff;
  box-shadow: 0 1px 1px rgba(0,0,0,.04);
}

.wp-notice-success {
  border-left-color: var(--wp-success);
}

.wp-notice-warning {
  border-left-color: var(--wp-warning);
}

.wp-notice-error {
  border-left-color: var(--wp-error);
}

.wp-notice p {
  margin: 0;
  font-size: 13px;
  line-height: 1.5;
}

/* ── Data Table ── */
.wp-list-table {
  width: 100%;
  border-collapse: collapse;
  border-spacing: 0;
  background: white;
  border: 1px solid var(--wp-border-light);
}

.wp-list-table thead th {
  padding: 8px 10px;
  text-align: left;
  font-size: 14px;
  font-weight: 400;
  color: var(--wp-text);
  border-bottom: 1px solid var(--wp-border-light);
  background: #f6f7f7;
  line-height: 1.4em;
}

.wp-list-table thead th.sortable {
  cursor: pointer;
}

.wp-list-table thead th.sortable:hover {
  background: #e9e9e9;
}

.wp-list-table tbody td {
  padding: 8px 10px;
  font-size: 13px;
  color: var(--wp-text);
  border-bottom: 1px solid var(--wp-border-light);
  vertical-align: top;
  line-height: 1.5em;
}

.wp-list-table tbody tr:hover {
  background-color: #f6f7f7;
}

.wp-list-table tbody tr:nth-child(even) {
  background-color: #f6f7f7;
}

.wp-list-table tbody tr:nth-child(even):hover {
  background-color: #eaeaea;
}

.wp-list-table .row-actions {
  font-size: 13px;
  padding: 2px 0 0;
  visibility: hidden;
  color: var(--wp-text-light);
}

.wp-list-table tbody tr:hover .row-actions {
  visibility: visible;
}

.wp-list-table .row-actions a {
  color: var(--wp-primary);
  text-decoration: none;
}

.wp-list-table .row-actions a:hover {
  color: var(--wp-primary-hover);
}

.wp-list-table .row-actions .delete a {
  color: var(--wp-error);
}

/* ── Table Navigation (Pagination) ── */
.wp-table-nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8px 0;
  color: var(--wp-text-light);
  font-size: 13px;
}

.wp-table-nav .displaying-num {
  font-size: 13px;
  color: var(--wp-text-light);
  font-style: italic;
}

.wp-pagination {
  display: flex;
  align-items: center;
  gap: 4px;
}

.wp-pagination button,
.wp-pagination .current-page {
  padding: 4px 8px;
  font-size: 13px;
  border: 1px solid var(--wp-border);
  background: #f6f7f7;
  cursor: pointer;
  min-width: 30px;
  text-align: center;
}

.wp-pagination button:hover {
  background: #dcdcde;
}

.wp-pagination button:disabled {
  opacity: 0.5;
  cursor: default;
}

.wp-pagination .current-page {
  width: 50px;
  text-align: center;
  border-color: var(--wp-primary);
}

/* ── Buttons ── */
.wp-button {
  display: inline-block;
  padding: 0 10px;
  font-size: 13px;
  line-height: 2.15;
  border: 1px solid var(--wp-border);
  border-radius: 3px;
  background: #f6f7f7;
  color: var(--wp-text);
  cursor: pointer;
  text-decoration: none;
  white-space: nowrap;
}

.wp-button:hover {
  background: #f0f0f1;
  border-color: #8c8f94;
  color: #1d2327;
}

.wp-button-primary {
  background: var(--wp-primary);
  border-color: var(--wp-primary);
  color: white;
}

.wp-button-primary:hover {
  background: var(--wp-primary-hover);
  border-color: var(--wp-primary-hover);
  color: white;
}

.wp-button-primary:disabled {
  opacity: 0.7;
  cursor: not-allowed;
}

.wp-button-danger {
  color: var(--wp-error);
  border-color: var(--wp-error);
}

.wp-button-danger:hover {
  background: var(--wp-error);
  color: white;
}

.wp-button-add {
  background: var(--wp-success);
  border-color: var(--wp-success);
  color: white;
}

.wp-button-add:hover {
  background: #3a9b41;
  border-color: #3a9b41;
  color: white;
}

/* ── Forms ── */
.wp-form-table {
  width: 100%;
  border-collapse: collapse;
}

.wp-form-field {
  margin-bottom: 16px;
}

.wp-form-field label {
  display: block;
  margin-bottom: 4px;
  font-size: 14px;
  font-weight: 600;
  color: var(--wp-text);
}

.wp-form-field .description {
  font-size: 13px;
  color: var(--wp-text-light);
  font-style: italic;
  margin-top: 4px;
}

.wp-form-input {
  width: 100%;
  max-width: 400px;
  padding: 4px 8px;
  font-size: 14px;
  line-height: 2;
  border: 1px solid var(--wp-border);
  border-radius: 4px;
  color: var(--wp-text);
  background: white;
}

.wp-form-input:focus {
  border-color: var(--wp-primary-focus);
  box-shadow: 0 0 0 1px var(--wp-primary-focus);
  outline: none;
}

.wp-form-input-wide {
  max-width: 100%;
}

.wp-form-select {
  width: auto;
  max-width: 400px;
  padding: 4px 24px 4px 8px;
  font-size: 14px;
  line-height: 2;
  border: 1px solid var(--wp-border);
  border-radius: 4px;
  color: var(--wp-text);
  background: white;
}

.wp-form-textarea {
  width: 100%;
  max-width: 600px;
  padding: 4px 8px;
  font-size: 14px;
  line-height: 1.6;
  border: 1px solid var(--wp-border);
  border-radius: 4px;
  color: var(--wp-text);
  background: white;
  resize: vertical;
}

.wp-form-textarea:focus,
.wp-form-select:focus {
  border-color: var(--wp-primary-focus);
  box-shadow: 0 0 0 1px var(--wp-primary-focus);
  outline: none;
}

.wp-form-error {
  color: var(--wp-error);
  font-size: 13px;
  margin-top: 4px;
}

/* ── Loading spinner ── */
.wp-spinner {
  display: inline-block;
  width: 16px;
  height: 16px;
  border: 2px solid rgba(255,255,255,0.3);
  border-radius: 50%;
  border-top-color: white;
  animation: wp-spin 0.6s linear infinite;
  margin-right: 6px;
  vertical-align: middle;
}

@keyframes wp-spin {
  to { transform: rotate(360deg); }
}

/* ── Dashboard Cards ── */
.wp-dashboard-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 20px;
}

.wp-dashboard-card {
  background: white;
  border: 1px solid var(--wp-border-light);
  border-radius: 0;
  box-shadow: 0 1px 1px rgba(0,0,0,.04);
}

.wp-dashboard-card-header {
  padding: 8px 12px;
  border-bottom: 1px solid var(--wp-border-light);
  background: #f6f7f7;
}

.wp-dashboard-card-header h3 {
  margin: 0;
  font-size: 14px;
  font-weight: 600;
  color: var(--wp-text);
}

.wp-dashboard-card-body {
  padding: 12px;
}

.wp-dashboard-card-count {
  font-size: 32px;
  font-weight: 300;
  color: var(--wp-primary);
}

/* ── Array Fields (Slots) ── */
.wp-slot-section {
  margin-top: 20px;
  border-top: 1px solid var(--wp-border-light);
  padding-top: 16px;
}

.wp-slot-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.wp-slot-header h3 {
  margin: 0;
  font-size: 14px;
  font-weight: 600;
}

.wp-slot-row {
  display: flex;
  gap: 8px;
  margin-bottom: 8px;
  align-items: center;
}

.wp-slot-row input {
  flex: 1;
}

/* ── Bulk Actions ── */
.wp-bulk-actions {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 0;
}

.wp-bulk-actions select {
  padding: 4px 24px 4px 8px;
  font-size: 14px;
  border: 1px solid var(--wp-border);
  border-radius: 3px;
}

/* ── Empty state ── */
.wp-empty-state {
  text-align: center;
  padding: 48px 20px;
  color: var(--wp-text-light);
  font-size: 14px;
}

/* ── Checkbox ── */
.wp-check-column {
  width: 30px;
  padding: 8px;
  vertical-align: middle;
}

.wp-check-column input[type="checkbox"] {
  width: 16px;
  height: 16px;
  cursor: pointer;
}
`;
}
```

- [ ] **Step 1.4: Add globals.css generation to generateAdminScaffold**

In `packages/analyzer/src/admin-scaffold-generator.ts`, add to the `generateAdminScaffold` function, after the dashboard page push (after the existing line that pushes dashboard page, around line 1614):

```typescript
  // Generate globals.css with WP admin styles
  pages.push({
    path: "app/globals.css",
    content: generateGlobalsCss(),
    type: "dashboard" as const,
  });
```

- [ ] **Step 1.5: Run test to verify it passes**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts -t "generates WP-admin-style globals.css"`
Expected: PASS

- [ ] **Step 1.6: Commit**

```bash
git add packages/analyzer/src/admin-scaffold-generator.ts packages/analyzer/tests/admin-scaffold-generator.test.ts
git commit -m "feat(admin): generate WP-admin-faithful globals.css with color palette and component classes"
```

---

## Task 2: WP Admin Layout with CSS Classes

**Files:**
- Modify: `packages/analyzer/src/admin-scaffold-generator.ts:930-1049` (generateAdminLayout)
- Test: `packages/analyzer/tests/admin-scaffold-generator.test.ts`

- [ ] **Step 2.1: Write failing test**

```typescript
// Add to packages/analyzer/tests/admin-scaffold-generator.test.ts

describe("WP-style admin layout", () => {
  it("generates layout with WP CSS classes instead of inline styles", () => {
    const analyses = [makeAnalysis({ fileName: "page-event-list.php" })];
    const tables = [makeTable("event")];

    const pages = generateAdminScaffold(analyses, tables);
    const layout = findPage(pages, "layout.tsx");

    expect(layout).toBeDefined();
    expect(layout!.content).toContain("wp-admin-sidebar");
    expect(layout!.content).toContain("wp-admin-menu-item");
    expect(layout!.content).toContain("wp-admin-content");
    expect(layout!.content).toContain('import "@/app/globals.css"');
    // Should NOT have inline style objects
    expect(layout!.content).not.toContain('backgroundColor: "#1f2937"');
  });

  it("generates layout with dashboard and resource links", () => {
    const analyses = [makeAnalysis({ fileName: "page-event-list.php" })];
    const tables = [makeTable("event")];

    const pages = generateAdminScaffold(analyses, tables);
    const layout = findPage(pages, "layout.tsx");

    expect(layout!.content).toContain("ダッシュボード");
    expect(layout!.content).toContain("/events");
  });
});
```

- [ ] **Step 2.2: Run test to verify it fails**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts -t "WP-style admin layout"`
Expected: FAIL — current layout uses inline styles

- [ ] **Step 2.3: Replace generateAdminLayout plain branch**

Replace the `fw === "plain"` branch of `generateAdminLayout` (lines 1001-1049) with WP CSS classes:

```typescript
  // Replace the entire else branch (plain) starting from the menuItemsJsx generation

  const menuItemsJsx = menuItems
    .map(
      (item) => `            <a href="${item.href}" className="wp-admin-menu-item">
              ${item.label}
            </a>`,
    )
    .join("\n");

  return `import type { ReactNode } from "react";
import "@/app/globals.css";

export default function AdminLayout({ children }: { children: ReactNode }) {
  return (
    <div style={{ display: "flex", minHeight: "100vh" }}>
      <nav className="wp-admin-sidebar">
        <div className="wp-admin-sidebar-header">管理画面</div>
        <div className="wp-admin-menu">
${menuItemsJsx}
        </div>
      </nav>

      <main className="wp-admin-content">
        {children}
      </main>
    </div>
  );
}
`;
```

- [ ] **Step 2.4: Run test to verify it passes**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts -t "WP-style admin layout"`
Expected: PASS

- [ ] **Step 2.5: Run full test suite**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts`
Expected: All tests pass

- [ ] **Step 2.6: Commit**

```bash
git add packages/analyzer/src/admin-scaffold-generator.ts packages/analyzer/tests/admin-scaffold-generator.test.ts
git commit -m "feat(admin): replace inline styles with WP CSS classes in admin layout"
```

---

## Task 3: WP-Style List Page with DataTable

**Files:**
- Modify: `packages/analyzer/src/admin-scaffold-generator.ts:146-283` (generateListPage), `1072-1185` (generateTableListPage)
- Test: `packages/analyzer/tests/admin-scaffold-generator.test.ts`

- [ ] **Step 3.1: Write failing test**

```typescript
describe("WP-style list page", () => {
  it("uses wp-list-table CSS class for data tables", () => {
    const analyses = [makeAnalysis({ fileName: "page-event-list.php" })];
    const tables = [makeTable("event")];

    const pages = generateAdminScaffold(analyses, tables);
    const listPage = findPage(pages, "events/page.tsx");

    expect(listPage!.content).toContain("wp-list-table");
    expect(listPage!.content).toContain("wp-admin-wrap");
    expect(listPage!.content).toContain("wp-admin-title");
    expect(listPage!.content).toContain("wp-button-primary");
    expect(listPage!.content).toContain("wp-table-nav");
    // No more inline style objects for layout
    expect(listPage!.content).not.toContain('backgroundColor: "#f3f4f6"');
  });

  it("includes pagination with item count", () => {
    const analyses = [makeAnalysis({ fileName: "page-event-list.php" })];
    const tables = [makeTable("event")];

    const pages = generateAdminScaffold(analyses, tables);
    const listPage = findPage(pages, "events/page.tsx");

    expect(listPage!.content).toContain("displaying-num");
    expect(listPage!.content).toContain("件");
  });
});
```

- [ ] **Step 3.2: Run test to verify it fails**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts -t "WP-style list page"`
Expected: FAIL

- [ ] **Step 3.3: Rewrite generateListPage plain branch**

Replace the plain branch (lines 219-282) of `generateListPage` with WP-style:

```typescript
  return `import { prisma } from "@/lib/db";
import Link from "next/link";

export const dynamic = "force-dynamic";

export default async function ${modelName}ListPage() {
  const items = await prisma.${camelModel}.findMany({
    orderBy: { id: "desc" },
    take: 100,
  });

  return (
    <div className="wp-admin-wrap">
      <div className="wp-admin-header">
        <h1 className="wp-admin-title">
          ${modelName} 一覧
          <Link href="/${pluralize(resource)}/new" className="wp-admin-title-action">
            新規追加
          </Link>
        </h1>
      </div>

      <div className="wp-table-nav">
        <span className="displaying-num">{items.length} 件</span>
      </div>

      <table className="wp-list-table">
        <thead>
          <tr>
${displayColumns.map((c) => `            <th>${c.comment ?? fieldLabel(c.name)}</th>`).join("\n")}
            <th>操作</th>
          </tr>
        </thead>
        <tbody>
          {items.map((item) => (
            <tr key={item.id}>
${displayColumns.map((c) => `              <td>{String(item.${c.name} ?? "")}</td>`).join("\n")}
              <td>
                <Link href={\`/${pluralize(resource)}/\${item.id}\`}>
                  編集
                </Link>
                {" | "}
                <Link href={\`/${pluralize(resource)}/\${item.id}\`} style={{ color: "var(--wp-error)" }}>
                  削除
                </Link>
              </td>
            </tr>
          ))}
        </tbody>
      </table>

      {items.length === 0 && (
        <p className="wp-empty-state">
          データがありません
        </p>
      )}
    </div>
  );
}
`;
```

- [ ] **Step 3.4: Rewrite generateTableListPage plain branch similarly**

Apply the same WP CSS class pattern to the `generateTableListPage` function (lines 1130-1177), using `wp-list-table`, `wp-admin-wrap`, `wp-admin-title`, `wp-table-nav` classes.

- [ ] **Step 3.5: Run test to verify it passes**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts -t "WP-style list page"`
Expected: PASS

- [ ] **Step 3.6: Run full test suite to check for regressions**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts`
Expected: All pass (update any tests checking for old inline styles)

- [ ] **Step 3.7: Commit**

```bash
git add packages/analyzer/src/admin-scaffold-generator.ts packages/analyzer/tests/admin-scaffold-generator.test.ts
git commit -m "feat(admin): WP-style list pages with wp-list-table and pagination"
```

---

## Task 4: WP-Style Form Pages

**Files:**
- Modify: `packages/analyzer/src/admin-scaffold-generator.ts:285-513` (form field JSX + formFromSpec), `1294-1514` (generateTableFormPage)
- Test: `packages/analyzer/tests/admin-scaffold-generator.test.ts`

- [ ] **Step 4.1: Write failing test**

```typescript
describe("WP-style form pages", () => {
  it("uses WP CSS classes for form fields", () => {
    const analyses = [
      makeAnalysis({
        fileName: "page-event.php",
        formSpec: {
          fields: [
            { name: "title", type: "text", label: "タイトル", required: true },
            { name: "status", type: "select", label: "ステータス", options: [{ value: "1", label: "公開" }, { value: "0", label: "非公開" }] },
          ],
          submitLabel: "登録",
        },
      }),
    ];
    const tables = [makeTable("event")];

    const pages = generateAdminScaffold(analyses, tables);
    const formPage = findPage(pages, "events/new/page.tsx");

    expect(formPage).toBeDefined();
    expect(formPage!.content).toContain("wp-form-field");
    expect(formPage!.content).toContain("wp-form-input");
    expect(formPage!.content).toContain("wp-form-select");
    expect(formPage!.content).toContain("wp-button-primary");
    expect(formPage!.content).toContain("wp-notice");
  });

  it("uses WP notice for error display", () => {
    const analyses = [
      makeAnalysis({
        fileName: "page-event.php",
        formSpec: {
          fields: [{ name: "title", type: "text", label: "タイトル" }],
          submitLabel: "保存",
        },
      }),
    ];
    const tables = [makeTable("event")];

    const pages = generateAdminScaffold(analyses, tables);
    const formPage = findPage(pages, "events/new/page.tsx");

    expect(formPage!.content).toContain("wp-notice-error");
  });
});
```

- [ ] **Step 4.2: Run test to verify it fails**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts -t "WP-style form pages"`
Expected: FAIL

- [ ] **Step 4.3: Rewrite generateFormFieldJsx with CSS classes**

Replace the `generateFormFieldJsx` function (lines 285-353):

```typescript
function generateFormFieldJsx(f: import("./php-analyzer.js").FormField, index: number): string {
  if (f.type === "hidden") return "";

  if (f.type === "select" && f.options) {
    const opts = f.options
      .map((o) => `              <option value="${o.value}">${o.label}</option>`)
      .join("\n");
    return `        <div className="wp-form-field">
          <label>${f.label}</label>
          <select
            className="wp-form-select"
            value={String(form.${f.name} ?? "")}
            onChange={(e) => setForm({ ...form, ${f.name}: e.target.value })}
          >
            <option value="">選択してください</option>
${opts}
          </select>
        </div>`;
  }

  if (f.type === "textarea") {
    return `        <div className="wp-form-field">
          <label>${f.label}</label>
          <textarea
            className="wp-form-textarea"
            rows={5}
            value={String(form.${f.name} ?? "")}
            onChange={(e) => setForm({ ...form, ${f.name}: e.target.value })}
          />
        </div>`;
  }

  if (f.type === "file") {
    return `        <div className="wp-form-field">
          <label>${f.label}</label>
          <input
            type="file"${f.accept ? `\n            accept="${f.accept}"` : ""}
            onChange={(e) => setFile(e.target.files?.[0] ?? null)}
          />
        </div>`;
  }

  const inputType = f.type === "datetime-local" || f.type === "date" || f.type === "url"
    ? f.type
    : f.type === "number" ? "number" : "text";

  const valueCoerce = f.type === "number"
    ? "Number(e.target.value)"
    : "e.target.value";

  return `        <div className="wp-form-field">
          <label>${f.label}</label>
          <input
            className="wp-form-input"
            type="${inputType}"
            value={String(form.${f.name} ?? "")}
            onChange={(e) => setForm({ ...form, ${f.name}: ${valueCoerce} })}${f.required ? '\n            required' : ""}${f.placeholder ? `\n            placeholder="${f.placeholder}"` : ""}
          />
        </div>`;
}
```

- [ ] **Step 4.4: Rewrite generateFormFromSpec with WP CSS classes**

Replace the wrapper JSX in `generateFormFromSpec` (the return template, lines 432-512). Key changes:
- Error display: use `<div className="wp-notice wp-notice-error"><p>{error}</p></div>`
- Form wrapper: use `<div className="wp-admin-wrap">`
- Title: use `<h1 className="wp-admin-title">`
- Submit button: use `<button className="wp-button wp-button-primary">`
- Cancel button: use `<button className="wp-button">`
- Spinner: use `<span className="wp-spinner" />`
- Array fields section: use `.wp-slot-section`, `.wp-slot-header`, `.wp-slot-row`
- Add/remove buttons: use `.wp-button-add`, `.wp-button-danger`

- [ ] **Step 4.5: Rewrite generateTableFormPage plain branch similarly**

Apply the same CSS class pattern to `generateTableFormPage` (lines 1432-1502).

- [ ] **Step 4.6: Run test to verify it passes**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts -t "WP-style form pages"`
Expected: PASS

- [ ] **Step 4.7: Run full test suite**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts`
Expected: All pass (fix any tests referencing old inline style patterns)

- [ ] **Step 4.8: Commit**

```bash
git add packages/analyzer/src/admin-scaffold-generator.ts packages/analyzer/tests/admin-scaffold-generator.test.ts
git commit -m "feat(admin): WP-style form pages with wp-form-field classes and notices"
```

---

## Task 5: WP-Style Detail + Dashboard Pages

**Files:**
- Modify: `packages/analyzer/src/admin-scaffold-generator.ts:749-928` (generateDetailPage, generateDashboardPage, generateTableDetailPage)
- Test: `packages/analyzer/tests/admin-scaffold-generator.test.ts`

- [ ] **Step 5.1: Write failing test**

```typescript
describe("WP-style detail and dashboard pages", () => {
  it("generates detail page with WP CSS classes", () => {
    const analyses: PhpFileAnalysis[] = [];
    const tables = [makeTable("event")];

    const pages = generateAdminScaffold(analyses, tables);
    const detail = pages.find((p) => p.path.includes("[id]/page.tsx"));

    expect(detail).toBeDefined();
    expect(detail!.content).toContain("wp-admin-wrap");
    expect(detail!.content).toContain("wp-admin-title");
    expect(detail!.content).toContain("wp-button-primary");
    expect(detail!.content).toContain("wp-button-danger");
  });

  it("generates dashboard with WP card classes", () => {
    const tables = [makeTable("event"), makeTable("information")];

    const pages = generateAdminScaffold([], tables);
    const dash = findPage(pages, "(admin)/page.tsx");

    expect(dash).toBeDefined();
    expect(dash!.content).toContain("wp-dashboard-grid");
    expect(dash!.content).toContain("wp-dashboard-card");
  });
});
```

- [ ] **Step 5.2: Run test to verify it fails**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts -t "WP-style detail and dashboard"`
Expected: FAIL

- [ ] **Step 5.3: Rewrite generateDetailPage plain branch**

Replace lines 1238-1284 (the plain branch) to use `wp-admin-wrap`, `wp-admin-title`, `wp-button-primary`, `wp-button-danger` CSS classes instead of inline styles.

- [ ] **Step 5.4: Rewrite generateDashboardPage plain branch**

Replace the plain branch of `generateDashboardPage` (around lines 889-927) to use `wp-dashboard-grid`, `wp-dashboard-card`, `wp-dashboard-card-header`, `wp-dashboard-card-body`, `wp-dashboard-card-count` CSS classes.

- [ ] **Step 5.5: Rewrite generateTableDetailPage plain branch similarly**

Apply WP CSS classes to the table-driven detail page generator.

- [ ] **Step 5.6: Run tests**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/admin-scaffold-generator.test.ts`
Expected: All pass

- [ ] **Step 5.7: Commit**

```bash
git add packages/analyzer/src/admin-scaffold-generator.ts packages/analyzer/tests/admin-scaffold-generator.test.ts
git commit -m "feat(admin): WP-style detail pages and dashboard cards"
```

---

## Task 6: Root Layout globals.css Import

**Files:**
- Modify: `packages/analyzer/src/admin-scaffold-generator.ts` — ensure generated root `app/layout.tsx` imports globals.css
- Modify: `apps/cli/src/commands/analyze-php.ts` — where root layout is written

- [ ] **Step 6.1: Find where root layout.tsx is generated**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && grep -n "layout.tsx" apps/cli/src/commands/analyze-php.ts | head -20`

- [ ] **Step 6.2: Add globals.css import to root layout**

In the root layout template (wherever `app/layout.tsx` is generated in analyze-php.ts), add `import "./globals.css";` at the top of the generated file.

- [ ] **Step 6.3: Verify by running analyze-php test or manual check**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run`
Expected: All tests pass

- [ ] **Step 6.4: Commit**

```bash
git add apps/cli/src/commands/analyze-php.ts
git commit -m "feat(admin): import globals.css in generated root layout"
```

---

## Task 7: Regenerate client-a Output

**Files:**
- Output: `output/client-a-final/` — regenerated with WP styles

- [ ] **Step 7.1: Run analyze-php to regenerate**

Run the analyze-php command to regenerate the client-a project with the new WP-style templates. The exact command depends on the PHP source and schema locations.

Run: `cd /home/o9oem/workspace/mine/wp-transfer && grep -r "client-a" apps/cli/ --include="*.ts" -l` to find the command invocation pattern.

Then run: `pnpm --filter wp-transfer-cli dev analyze-php --dir <php-source-dir> --schema <schema-path> --output output/client-a-final`

- [ ] **Step 7.2: Verify globals.css exists in output**

Run: `ls -la output/client-a-final/app/globals.css`
Expected: File exists with WP admin styles

- [ ] **Step 7.3: Verify layout uses CSS classes**

Run: `head -20 output/client-a-final/app/\(admin\)/layout.tsx`
Expected: Contains `wp-admin-sidebar`, no inline backgroundColor styles

- [ ] **Step 7.4: Commit regenerated output**

```bash
git add output/client-a-final/
git commit -m "chore: regenerate client-a-final with WP admin CSS"
```

---

## Task 8: Enhanced E2E Test Generation — Auth Tests

**Files:**
- Modify: `packages/analyzer/src/verify-generator.ts:171-198` (generateAuthSpec)
- Test: `packages/analyzer/tests/verify-generator.test.ts`

- [ ] **Step 8.1: Write failing test**

```typescript
// In packages/analyzer/tests/verify-generator.test.ts

describe("enhanced auth spec", () => {
  it("generates auth tests with cookie-based session validation", () => {
    const result = generateVerifyScaffold({
      postSlugs: [],
      categorySlugs: [],
      hasAuth: true,
      tableNames: ["event"],
    });

    const authSpec = result.find((f) => f.path === "e2e/auth.spec.ts");
    expect(authSpec).toBeDefined();
    expect(authSpec!.content).toContain("session");
    expect(authSpec!.content).toContain("401");
    expect(authSpec!.content).toContain("redirect");
  });
});
```

- [ ] **Step 8.2: Run test to verify it passes or needs adjustment**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/verify-generator.test.ts -t "enhanced auth spec"`

- [ ] **Step 8.3: Enhance generateAuthSpec to test session validation**

If the existing auth spec doesn't test response body or cookie validity, enhance it to:
- Verify unauthenticated requests get 401 or redirect to login
- Verify authenticated requests return valid session data
- Test session expiry handling

- [ ] **Step 8.4: Run tests**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/verify-generator.test.ts`
Expected: All pass

- [ ] **Step 8.5: Commit**

```bash
git add packages/analyzer/src/verify-generator.ts packages/analyzer/tests/verify-generator.test.ts
git commit -m "feat(verify): enhanced auth E2E tests with session validation"
```

---

## Task 9: Enhanced CRUD Test Generation

**Files:**
- Modify: `packages/analyzer/src/verify-generator.ts:276-345` (generateMigrationCrudSpec)
- Test: `packages/analyzer/tests/verify-generator.test.ts`

- [ ] **Step 9.1: Write failing test**

```typescript
describe("enhanced CRUD spec", () => {
  it("generates CRUD tests with response body validation", () => {
    const result = generateVerifyScaffold({
      postSlugs: [],
      categorySlugs: [],
      hasAuth: true,
      tableNames: ["event", "information"],
      phpAnalyses: [
        { fileName: "insert.php", dbOperations: [{ type: "INSERT", table: "event" }], inputParams: [{ name: "title", source: "POST" }] },
        { fileName: "update.php", dbOperations: [{ type: "UPDATE", table: "event" }], inputParams: [{ name: "title", source: "POST" }] },
        { fileName: "delete.php", dbOperations: [{ type: "DELETE", table: "event" }], inputParams: [] },
      ],
    });

    const crudSpec = result.find((f) => f.path === "e2e/migration-crud.spec.ts");
    expect(crudSpec).toBeDefined();
    // Should validate response body structure
    expect(crudSpec!.content).toContain("expect(body");
    // Should test create → read → update → delete flow
    expect(crudSpec!.content).toContain("POST");
    expect(crudSpec!.content).toContain("PUT");
    expect(crudSpec!.content).toContain("DELETE");
  });

  it("generates tests for all 5 domains", () => {
    const result = generateVerifyScaffold({
      postSlugs: [],
      categorySlugs: [],
      hasAuth: true,
      tableNames: ["event", "event_slot", "information", "lottery", "user"],
      phpAnalyses: [
        { fileName: "insert.php", dbOperations: [{ type: "INSERT", table: "event" }], inputParams: [] },
        { fileName: "insert_event_slot.php", dbOperations: [{ type: "INSERT", table: "event_slot" }], inputParams: [] },
        { fileName: "insert_information.php", dbOperations: [{ type: "INSERT", table: "information" }], inputParams: [] },
        { fileName: "lottery-update.php", dbOperations: [{ type: "UPDATE", table: "lottery" }], inputParams: [] },
        { fileName: "user-blacklist.php", dbOperations: [{ type: "UPDATE", table: "user" }], inputParams: [] },
      ],
    });

    const crudSpec = result.find((f) => f.path === "e2e/migration-crud.spec.ts");
    expect(crudSpec!.content).toContain("/api/events");
    expect(crudSpec!.content).toContain("/api/event_slot");
    expect(crudSpec!.content).toContain("/api/information");
    expect(crudSpec!.content).toContain("/api/lotteries");
  });
});
```

- [ ] **Step 9.2: Run test to verify**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/verify-generator.test.ts -t "enhanced CRUD spec"`

- [ ] **Step 9.3: Enhance generateMigrationCrudSpec**

Modify `generateMigrationCrudSpec` to:
- Add response body validation (check `body.id`, `body.title` exist after POST)
- Chain create → read → update → delete in sequence using `test.describe.serial`
- Store created ID for subsequent operations
- Validate DB state through GET after mutations

- [ ] **Step 9.4: Run tests**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/verify-generator.test.ts`
Expected: All pass

- [ ] **Step 9.5: Commit**

```bash
git add packages/analyzer/src/verify-generator.ts packages/analyzer/tests/verify-generator.test.ts
git commit -m "feat(verify): enhanced CRUD E2E tests with response body validation and sequential flow"
```

---

## Task 10: Enhanced Logic + Form Tests

**Files:**
- Modify: `packages/analyzer/src/verify-generator.ts:455-606` (generateMigrationLogicSpec, generateMigrationFormSpec)
- Test: `packages/analyzer/tests/verify-generator.test.ts`

- [ ] **Step 10.1: Write failing test**

```typescript
describe("enhanced logic spec", () => {
  it("generates state transition tests with before/after verification", () => {
    const result = generateVerifyScaffold({
      postSlugs: [],
      categorySlugs: [],
      hasAuth: true,
      tableNames: ["event", "user"],
      phpAnalyses: [
        { fileName: "event-stop.php", dbOperations: [{ type: "UPDATE", table: "event" }], inputParams: [] },
        { fileName: "event-restoration.php", dbOperations: [{ type: "UPDATE", table: "event" }], inputParams: [] },
        { fileName: "user-blacklist.php", dbOperations: [{ type: "UPDATE", table: "user" }], inputParams: [] },
        { fileName: "user-blacklist-out.php", dbOperations: [{ type: "UPDATE", table: "user" }], inputParams: [] },
      ],
    });

    const logicSpec = result.find((f) => f.path === "e2e/migration-logic.spec.ts");
    expect(logicSpec).toBeDefined();
    // Should verify state before and after transition
    expect(logicSpec!.content).toContain("status");
    expect(logicSpec!.content).toContain("blacklist");
  });
});

describe("enhanced form spec", () => {
  it("generates form tests that check WP CSS classes", () => {
    const result = generateVerifyScaffold({
      postSlugs: [],
      categorySlugs: [],
      hasAuth: true,
      tables: [
        {
          name: "event",
          columns: [
            { name: "id", type: "Int", nullable: false, isPrimary: true, isAutoIncrement: true },
            { name: "title", type: "String", nullable: false },
          ],
        },
      ],
    });

    const formSpec = result.find((f) => f.path === "e2e/migration-form.spec.ts");
    expect(formSpec).toBeDefined();
    expect(formSpec!.content).toContain("wp-form-field");
  });
});
```

- [ ] **Step 10.2: Run test**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/verify-generator.test.ts -t "enhanced logic spec"`

- [ ] **Step 10.3: Enhance logic test generation**

Update `generateMigrationLogicSpec` to include GET verification before and after state transitions (verify status=0 → stop → verify status=1 → restore → verify status=0).

- [ ] **Step 10.4: Update form test generation for WP classes**

Update `generateMigrationFormSpec` to check for `.wp-form-field`, `.wp-list-table`, `.wp-admin-title` selectors in Playwright locators.

- [ ] **Step 10.5: Run tests**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run packages/analyzer/tests/verify-generator.test.ts`
Expected: All pass

- [ ] **Step 10.6: Commit**

```bash
git add packages/analyzer/src/verify-generator.ts packages/analyzer/tests/verify-generator.test.ts
git commit -m "feat(verify): enhanced logic state-transition and WP-class form E2E tests"
```

---

## Task 11: CLI Run Command — Migration Coverage Report

**Files:**
- Modify: `apps/cli/src/commands/run.ts`
- Test: `apps/cli/tests/run.test.ts` (create if needed)

- [ ] **Step 11.1: Write failing test**

```typescript
// apps/cli/tests/run.test.ts
import { describe, it, expect } from "vitest";
import { generateCoverageReport } from "../src/commands/run.js";

describe("migration coverage report", () => {
  it("generates markdown report with PHP script coverage", () => {
    const report = generateCoverageReport({
      phpScripts: 22,
      testsGenerated: 18,
      testsPassed: 15,
      testsFailed: 3,
      domains: [
        { name: "イベント管理", scripts: 7, tested: 7, passed: 5 },
        { name: "スロット管理", scripts: 3, tested: 3, passed: 3 },
        { name: "お知らせ管理", scripts: 8, tested: 6, passed: 5 },
        { name: "抽選管理", scripts: 1, tested: 1, passed: 1 },
        { name: "ユーザー管理", scripts: 2, tested: 1, passed: 1 },
      ],
    });

    expect(report).toContain("移行カバレッジ");
    expect(report).toContain("22");
    expect(report).toContain("81%"); // 18/22
    expect(report).toContain("イベント管理");
  });
});
```

- [ ] **Step 11.2: Run test to verify it fails**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run apps/cli/tests/run.test.ts`
Expected: FAIL — function doesn't exist

- [ ] **Step 11.3: Implement generateCoverageReport**

Add to `apps/cli/src/commands/run.ts`:

```typescript
export interface CoverageInput {
  phpScripts: number;
  testsGenerated: number;
  testsPassed: number;
  testsFailed: number;
  domains: Array<{
    name: string;
    scripts: number;
    tested: number;
    passed: number;
  }>;
}

export function generateCoverageReport(input: CoverageInput): string {
  const coverage = Math.round((input.testsGenerated / input.phpScripts) * 100);
  const passRate = input.testsGenerated > 0
    ? Math.round((input.testsPassed / input.testsGenerated) * 100)
    : 0;

  const domainRows = input.domains
    .map((d) => `| ${d.name} | ${d.scripts} | ${d.tested} | ${d.passed} | ${d.scripts === d.passed ? "✅" : "⚠️"} |`)
    .join("\n");

  return `# 移行カバレッジレポート

## サマリー
- **PHPスクリプト総数**: ${input.phpScripts}
- **テスト生成済み**: ${input.testsGenerated} (${coverage}%)
- **テストパス**: ${input.testsPassed} / ${input.testsGenerated} (${passRate}%)
- **テスト失敗**: ${input.testsFailed}

## ドメイン別

| ドメイン | スクリプト数 | テスト済み | パス | 状態 |
|---------|------------|----------|------|------|
${domainRows}

## 判定
${coverage >= 100 && passRate >= 100 ? "✅ **移行完了** — 全スクリプトのテストがパスしています" : `⚠️ **移行未完了** — カバレッジ ${coverage}%、パス率 ${passRate}%`}
`;
}
```

- [ ] **Step 11.4: Run test to verify it passes**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run apps/cli/tests/run.test.ts`
Expected: PASS

- [ ] **Step 11.5: Commit**

```bash
git add apps/cli/src/commands/run.ts apps/cli/tests/run.test.ts
git commit -m "feat(cli): add migration coverage report generation"
```

---

## Task 12: CLI Run Command — Error Recovery Guidance

**Files:**
- Modify: `apps/cli/src/commands/run.ts:13-27` (runStep)

- [ ] **Step 12.1: Add error-specific guidance**

Modify `runStep` to detect common failure patterns and provide actionable guidance:

```typescript
const ERROR_GUIDANCE: Record<string, string> = {
  "EADDRINUSE": "Port already in use. Run: docker compose down && docker compose up -d --wait",
  "P1001": "Database connection failed. Check: docker compose ps, verify DB is healthy",
  "P3009": "Migration failed. Run: npx prisma migrate reset (WARNING: drops data)",
  "ENOENT": "Command not found. Run: npm ci to install dependencies",
  "ENOMEM": "Out of memory. Increase Docker memory limit in Docker Desktop settings",
};

function getErrorGuidance(error: Error): string | undefined {
  const msg = error.message;
  for (const [pattern, guidance] of Object.entries(ERROR_GUIDANCE)) {
    if (msg.includes(pattern)) return guidance;
  }
  return undefined;
}
```

Update `runStep` to use `getErrorGuidance` when a step fails:

```typescript
function runStep(name: string, command: string, cwd: string, optional = false): boolean {
  consola.start(name);
  try {
    execSync(command, { cwd, stdio: "inherit", timeout: 300_000 });
    consola.success(name);
    return true;
  } catch (error) {
    if (optional) {
      consola.warn(`${name} (skipped: ${(error as Error).message})`);
      return true;
    }
    consola.fail(name);
    const guidance = getErrorGuidance(error as Error);
    if (guidance) {
      consola.info(`💡 ${guidance}`);
    }
    return false;
  }
}
```

- [ ] **Step 12.2: Run full test suite**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run`
Expected: All pass

- [ ] **Step 12.3: Commit**

```bash
git add apps/cli/src/commands/run.ts
git commit -m "feat(cli): add error recovery guidance for common failures"
```

---

## Task 13: Regenerate and Full Verification

- [ ] **Step 13.1: Run full test suite**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && npx vitest run`
Expected: All tests pass

- [ ] **Step 13.2: Run typecheck**

Run: `cd /home/o9oem/workspace/mine/wp-transfer && pnpm -r typecheck`
Expected: No errors

- [ ] **Step 13.3: Regenerate client-a-final output**

Run the analyze-php command to regenerate with all new WP-style templates.

- [ ] **Step 13.4: Verify generated output quality**

Check:
- `output/client-a-final/app/globals.css` exists with WP colors
- `output/client-a-final/app/(admin)/layout.tsx` uses WP CSS classes
- `output/client-a-final/app/(admin)/events/page.tsx` uses `wp-list-table`
- `output/client-a-final/app/(admin)/events/new/page.tsx` uses `wp-form-field`
- E2E test files in `output/client-a-final/e2e/` include enhanced tests

- [ ] **Step 13.5: Final commit**

```bash
git add -A
git commit -m "chore: regenerate client-a-final with WP admin faithful UI + enhanced E2E tests"
```

---

## Task Dependency Graph

```
Task 1 (globals.css) ──┐
Task 2 (layout)     ───┤
Task 3 (list page)  ───┤──> Task 6 (root layout import) ──> Task 7 (regenerate)
Task 4 (form page)  ───┤
Task 5 (detail+dash)───┘
                                                              │
Task 8 (auth E2E)  ───┐                                      │
Task 9 (CRUD E2E)  ───┤──> Task 10 (logic+form E2E) ─────────┤
                       │                                      │
Task 11 (coverage) ───┤                                      │
Task 12 (error guide)──┘──────────────────────────────────────┤
                                                              v
                                                        Task 13 (verify all)
```
